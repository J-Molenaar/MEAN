<!DOCTYPE html>
<html>
  	<head>
    		<meta charset="utf-8">
    		<title>Quoting Dojo!</title>
		    <link rel="style" href="/static/style.css">
    		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">

    </head>
 	<body>
        <div id="wrapper">
            <h1>Here are the awesome quotes!</h1>
            <br>
            <div id="for_errors">
              <% for( var i in all_quotes ) { %>
                  <p>"<%= all_quotes[i].quote %>"</p>
                  <p><%= all_quotes[i].name %> at <%= all_quotes[i].date %></p>
                  <% if( i < all_quotes.length - 1 ) { %>
                    <hr />
                  <% } %>
              <% } %>
            </div>
            <a href="/"><button>Home</button></a>
        </div> <!-- end wrapper -->
    </body>
</html>

<style media="screen">
    #wrapper{
        margin-top: 100px;
        margin-left: 250px;
        margin-right: 250px;
        border: 1px solid black;
        padding: 10px 10px 10px 10px;
    }
</style>
